# ProductService - sample project for unit testing practice

This sample Spring Boot project includes example controller, service, repository and unit/integration tests.
Run with:

mvn clean test
mvn jacoco:report
Open coverage report at target/site/jacoco/index.html
